<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <title>Éter Party - Ingresso</title>
 
  <link href="https://fonts.googleapis.com/css2?family=League+Gothic&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'League Gothic', Arial, sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
      font-size: 1.4em;
    }

    .ticket {
      background-image: url('<%= @banner_data_url %>');
      background-size: cover;
      background-position: center;
      text-align: center;
      color: #333333;
      max-width: 1344px;
      padding: 23.52px 40.32px 16.8px;
      border-radius: 8px;
      box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
      display: flex;
      flex-direction: column;
      height: 840px;
    }

    .header, .footer {
      font-size: 1.512rem;
      letter-spacing: 0.15em;
      font-weight: 500;
    }

    .content {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
    }

    .title {
      font-size: 6.048rem;
      font-weight: 500;
      margin-bottom: 30px;
      letter-spacing: 0.05em;
      transform: scaleY(1.2);
    }
    
    .subtitle {
      font-size: 1.596rem;
      font-weight: 500;
      letter-spacing: 0.07em;
      margin-bottom: 14px;
    }

    .date {
      font-size: 4.536rem;
      letter-spacing: 0.08em;
    }

    .note {
      font-size: 1.512rem;
      margin-top: 66px;
      color: #333333;
      transform: scaleY(0.7);
    }

    .qrcode-container {
      display: flex;
      justify-content: center;
      width: 100%;
      height: 100%;
    }

    .qrcode {
      margin-top: 12px;
      width: 400px;
      height: 400px;
    }
  </style>
</head>
<body>
  <div class="ticket">
    <div class="header"><%= @ticket.location.upcase %> | <%= @ticket.city.upcase %> - <%= @ticket.state.upcase %></div>
    <div class="qrcode-container">
      <img class="qrcode" src="<%= @qr_code_data_url %>" alt="Éter Party">
    </div>

    <div class="content">
      <div class="title"><%= @ticket.name.upcase %></div>
      <div class="subtitle"><%= @ticket.description.upcase %></div>
      <div class="date"><%=I18n.l(@ticket.date, format: "%d DE %B").upcase %></div>
      <div class="footer note"><%= @ticket.footer_description.upcase %></div>
    </div>
  </div>
</body>
</html>
